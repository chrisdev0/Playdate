package apilayer;

import spark.Request;
import spark.Response;

public class GetPlaygroundHandler implements RequestHandler {

    @Override
    public Object handle(Request request, Response response) {
        throw new RuntimeException("//todo");
    }
}
